import { RouteChainDirective } from './route-chain.directive';

describe('RouteGunDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteChainDirective();
    expect(directive).toBeTruthy();
  });
});
