import { ChainDirective } from './chain.directive';

describe('ChainDirective', () => {
  it('should create an instance', () => {
    const directive = new ChainDirective();
    expect(directive).toBeTruthy();
  });
});
