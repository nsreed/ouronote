import { GetDirective } from './get.directive';

describe('GetDirective', () => {
  it('should create an instance', () => {
    const directive = new GetDirective();
    expect(directive).toBeTruthy();
  });
});
