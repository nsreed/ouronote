import { GunMapDirective } from './gun-map.directive';

describe('GunMapDirective', () => {
  it('should create an instance', () => {
    const directive = new GunMapDirective();
    expect(directive).toBeTruthy();
  });
});
