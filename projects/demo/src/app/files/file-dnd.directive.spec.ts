import { FileDndDirective } from './file-dnd.directive';

describe('FileDndDirective', () => {
  it('should create an instance', () => {
    const directive = new FileDndDirective();
    expect(directive).toBeTruthy();
  });
});
