import { PaperEditDirective } from './paper-edit.directive';

describe('PaperEditDirective', () => {
  it('should create an instance', () => {
    const directive = new PaperEditDirective();
    expect(directive).toBeTruthy();
  });
});
