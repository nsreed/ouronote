import { PaperScopeDirective } from './paper-scope.directive';

describe('PaperScopeDirective', () => {
  it('should create an instance', () => {
    const directive = new PaperScopeDirective();
    expect(directive).toBeTruthy();
  });
});
