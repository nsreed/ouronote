import { RouteMessageDirective } from './route-message.directive';

describe('RouteMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteMessageDirective();
    expect(directive).toBeTruthy();
  });
});
