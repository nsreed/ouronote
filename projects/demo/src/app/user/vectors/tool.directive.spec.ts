import { ToolDirective } from './tool.directive';

describe('ToolDirective', () => {
  it('should create an instance', () => {
    const directive = new ToolDirective();
    expect(directive).toBeTruthy();
  });
});
