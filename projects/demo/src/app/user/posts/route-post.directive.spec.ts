import { RoutePostDirective } from './route-post.directive';

describe('RoutePostDirective', () => {
  it('should create an instance', () => {
    const directive = new RoutePostDirective();
    expect(directive).toBeTruthy();
  });
});
