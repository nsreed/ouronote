version: "3.0"
services:
  devcontainer:
    image: mcr.microsoft.com/devcontainers/typescript-node:0-18
    # build:
    #   context: .
    #   dockerfile: dockerfile
    # volumes:
    #   - ouronote-cached:/cached
    command: sleep infinity
#  gunrelay:
 #   build:
  #    context: ..
   #   image: app-server/DOCKERFILE
